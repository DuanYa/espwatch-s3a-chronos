# Waveshare ESP32-S3-Touch-LCD-1.69 (ESPS3_1_69) 外设引脚接线详细说明

本文档分析了当前项目中默认编译环境（`lolin_s3_mini_1_69` / `ESPS3_1_69`）下的ESP32-S3芯片与各项板载及外接外设的引脚连接关系。

考虑到开发板既有出厂硬件默认接线（Waveshare stock defaults），又有本项目中定制开发的智能手表固件（ESPWatch-S3A3）中的软件配置（部分引脚经过了重定义或复用），下面将分别予以列出，以便于硬件连接与开发调试。

---

## 1. 屏幕 (SPI 接口)
使用的是 SPI3_HOST。屏幕驱动芯片通常为 **ST7789V2**。

| 外设/功能 | 本项目固件引脚 (`ESPS3_1_69`) | 微雪出厂硬件默认引脚 | 说明 |
| :--- | :--- | :--- | :--- |
| **SCLK / SCK** | GPIO 7 | GPIO 6 | SPI 时钟信号 |
| **MOSI / DIN** | GPIO 5 | GPIO 7 | SPI 数据输入 (主出从入) |
| **MISO / DOUT**| -1 (未使用) | -1 (未使用) | 未使用 |
| **DC** | GPIO 6 | GPIO 4 | 数据 / 命令控制引脚 |
| **CS** | GPIO 8 | GPIO 5 | 片选控制引脚 |
| **RST** | GPIO 4 | GPIO 8 | 复位引脚 |
| **BL (背光)** | GPIO 14 | GPIO 15 | LCD 背光亮度控制 (PWM) |

---

## 2. 触摸屏 (I2C 接口)
板载触摸控制器通常为 **CST816T** / **FT6X36**，I2C 7位地址通常为 `0x15` 或 `0x38`。

| 外设/功能 | 本项目固件引脚 (`ESPS3_1_69`) | 微雪出厂硬件默认引脚 | 说明 |
| :--- | :--- | :--- | :--- |
| **I2C_SDA** | GPIO 1 | GPIO 11 | I2C 数据线 |
| **I2C_SCL** | GPIO 2 | GPIO 10 | I2C 时钟线 |
| **TP_INT** | GPIO 10 | GPIO 14 | 触摸中断引脚 |
| **TP_RST** | GPIO 11 | GPIO 13 | 触摸复位引脚 |

---

## 3. 惯性测量单元 IMU (I2C 接口)
板载 6 轴传感器通常为 **QMI8658C**（包含 3 轴加速度计 + 3 轴陀螺仪），I2C 地址为 `0x6B`。
它与触摸屏等 I2C 外设共享同一个 I2C 总线。

| 外设/功能 | 本项目固件引脚 (`ESPS3_1_69`) | 微雪出厂硬件默认引脚 | 说明 |
| :--- | :--- | :--- | :--- |
| **I2C_SDA** | GPIO 1 | GPIO 11 | 共享 I2C 数据线 |
| **I2C_SCL** | GPIO 2 | GPIO 10 | 共享 I2C 时钟线 |
| **IMU_INT** | GPIO 38 | GPIO 38 | IMU 中断引脚 (INT2/INT1) |

---

## 4. 心率血氧传感器 (I2C 接口)
支持外接 **MAX30105** 或 **MAX30102** 光学传感器，I2C 7位地址为 `0x57`。
该传感器可以通过开发板上的 I2C 扩展接口接入并共享 I2C 总线。

| 外设/功能 | 本项目固件引脚 (`ESPS3_1_69`) | 微雪出厂扩展接口引脚 | 说明 |
| :--- | :--- | :--- | :--- |
| **I2C_SDA** | GPIO 1 | GPIO 11 | 共享 I2C 数据线 |
| **I2C_SCL** | GPIO 2 | GPIO 10 | 共享 I2C 时钟线 |
| **VCC** | 3.3V | 3.3V | 电源输入 |
| **GND** | GND | GND | 接地 |
| **INT (可选)**| 悬空/未连接 | 悬空/未连接 | 中断引脚，当前驱动库不强制使用 |

---

## 5. 音频解码芯片与喇叭 (I2C + I2S 接口)
为了支持高质量音频输出与喇叭（Speaker）播放，系统集成了 **ES8311** 低功耗单声道音频 Codec 芯片。

### 5.1 控制接口 (I2C)
ES8311 芯片的控制引脚（用于设置音量、配置采样率等）通过 I2C 接口与 ESP32-S3 连接，I2C 地址为 `0x18`。

| 外设/功能 | 本项目固件引脚 (`ESPS3_1_69`) | 默认/推荐连接引脚 | 说明 |
| :--- | :--- | :--- | :--- |
| **I2C_SDA** | GPIO 1 | GPIO 11 / 共享 | I2C 数据线 |
| **I2C_SCL** | GPIO 2 | GPIO 10 / 共享 | I2C 时钟线 |

### 5.2 音频数据接口 (I2S)
音频数据通过 I2S 标准串行接口进行高速双向传输。

| 功能信号 | 典型推荐引脚 | 说明 |
| :--- | :--- | :--- |
| **MCLK (主时钟)** | GPIO 3 / GPIO 16 | 供 ES8311 使用的主时钟（如Codec处于Master模式则非必连，通常用 ESP32-S3 输出） |
| **BCLK (位时钟)** | GPIO 15 / GPIO 9 | I2S 通信的位同步时钟信号 |
| **LRCK / WS** | GPIO 16 / GPIO 10 | I2S 帧时钟 / 左右声道选择信号 |
| **I2S_DOUT / DACDAT**| GPIO 17 / GPIO 11 | ESP32-S3 输出给 ES8311 的音频数据信号（用于喇叭播放） |
| **I2S_DIN / ADCDAT** | GPIO 18 / GPIO 12 | ES8311 输入给 ESP32-S3 的录音数据信号（用于麦克风输入） |

### 5.3 喇叭与功放连接
- **喇叭 (Speaker)**：ES8311 的模拟差分输出引脚 (`OUT1P` / `OUT1N` 或 `LOUT` / `ROUT`) 连接至音频功率放大器（如 NS4150, PAM8302 等），最后由功放驱动 8Ω / 1W 左右的小喇叭。

---

## 6. 麦克风 (MIC)
麦克风采集方案可采用模拟麦克风或数字麦克风：

### 6.1 方案 A：模拟麦克风（通过 ES8311）
模拟驻极体或硅麦克风连接到 **ES8311** 芯片的模拟输入端（`MIC1P` / `MIC1N`），经过 ES8311 内部的可编程增益放大器 (PGA) 和高分辨率 ADC 转化为数字信号，再通过 I2S 总线的 **I2S_DIN** 引脚输入给 ESP32-S3。

### 6.2 方案 B：数字 I2S 麦克风（如 INMP441 / MSM261S4030H0）
若不经过音频解码芯片，亦可将数字麦克风直接连接到 ESP32-S3 预留的 I2S 输入引脚上：

| 麦克风引脚 | ESP32-S3 连接引脚 | 说明 |
| :--- | :--- | :--- |
| **SCK (BCLK)** | GPIO 15 / GPIO 9 | 时钟信号 |
| **WS (LRCK)** | GPIO 16 / GPIO 10 | 声道选择 / 帧同步 |
| **SD (SDIN)** | GPIO 18 / GPIO 12 | 串行数据输出，接 ESP32-S3 I2S 数据输入 |
| **L/R** | GND 或 VCC | 设置左声道/右声道 |

---

## 7. 按键与电源管理
开发板包含若干板载及系统控制按键：

| 按键功能 | ESP32-S3 引脚 | 出厂对应硬件 | 软件实现与操作说明 |
| :--- | :--- | :--- | :--- |
| **BUTTON_HOME (主页/电源唤醒键)**| GPIO 0 | 板载 BOOT 键 / 实体按键 | - **单击**：亮屏 / 返回主页<br>- **双击**：进入应用列表<br>- **长按**：进入表盘切换选择面<br>- **深度休眠唤醒**：配置为 ext0 外部唤醒源 |
| **PWR / Key2 (电源管理/多功能)** | SYS_OUT (GPIO 40) <br> SYS_EN (GPIO 41) | Onboard Power circuit | 连接硬件电源保持与控制网络。SYS_OUT 引脚默认为高电平，按下按键时被拉低以检测按键状态。SYS_EN 控制电源保持。 |
| **RST (复位按键)** | CHIP_PU (EN) | 板载 RST 键 | 硬件复位按键，直接将 EN 引脚拉低，使芯片复位。 |

---

## 8. 震动马达
智能手表固件中提供了丰富的震动反馈，支持通知提醒、来电警报、系统反馈等。

| 功能 | ESP32-S3 引脚 | 说明 |
| :--- | :--- | :--- |
| **VIBRATION_PIN** | GPIO 9 | 震动马达控制引脚。置高电平 (High) 开启震动，置低电平 (Low) 停止。 |

---

## 9. 其他板载外设 (RTC, Buzzer, Battery ADC)

| 板载外设 | 设备型号 | 接口 / 引脚 | 说明 |
| :--- | :--- | :--- | :--- |
| **RTC 实时时钟** | PCF85063 | I2C (地址: `0x51`) <br> SCL: GPIO 2 / SDA: GPIO 1 <br> **中断引脚 RTC_INT**: GPIO 39 | 用于在系统深度睡眠或掉电时保持高精度时间。 |
| **蜂鸣器 (Buzzer)** | 无源蜂鸣器 | **Buzz**: GPIO 42 (PWM 驱动) | 可用于输出各种频率 of 系统提示音或简易旋律。 |
| **电池电压采样 (ADC)** | 电阻分压电路 / ETA6098 | **BAT_ADC**: GPIO 1 | 经 200K 与 100K 阻值分压接入 ESP32-S3 的 ADC，实时读取并换算电池电压：$V_{BAT} = V_{ADC} \times 3$。 |

---

> 备注：
> 1. 以上本项目固件引脚数据参考自库中的 `hal/esp32/displays/pins.h` 及 `hal/esp32/app_hal.cpp` 源码。
> 2. 用户在进行硬件二次开发或接线时，请务必核对编译选项以及对应的硬件版本，以防止引脚冲突造成芯片或外设烧毁。
